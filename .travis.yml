matrix:
  include:
    - language: java
      sudo: false
      before_script:
        - cd product-store
      script:
        - mvn clean verify

    - language: node_js
      node_js:
        - '4.2'
      sudo: required
      dist: trusty
      before_script:
        - cd product-store-web
      script:
        - npm install
        - ng build