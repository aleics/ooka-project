matrix:
  include:
    - language: java
      jdk:
        - openjdk8
      sudo: false
      before_script:
        - cd product-store
      script:
        - mvn clean verify

    - language: node_js
      node_js:
        - '5.6'
      sudo: required
      dist: trusty
      before_script:
        - cd product-store-web
      script:
        - npm install
        - ng build